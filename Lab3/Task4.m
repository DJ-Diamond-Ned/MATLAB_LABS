clc                 % очищает окно команд MATLAB
clear               % удаляет все переменные из памяти

% Матрица атомного состава веществ
% строки — химические элементы
% столбцы — вещества (Na2CO3, HNO3, NaNO3, H2O, CO2, CaO, Ca(NO3)2)
A = [2 0 1 0 0 0 0;  % Na
     1 0 0 0 1 0 0;  % C
     3 3 3 1 2 1 6;  % O
     0 1 0 2 0 0 0;  % H
     0 1 1 0 0 0 2;  % N
     0 0 0 0 0 1 1]; % Ca

rankA = rank(A)     % вычисляет ранг матрицы (число независимых уравнений)

normA = norm(A)     % вычисляет норму матрицы (характеристика её размера)

B = null(A)         % находит базис решений однородной системы Ax = 0
                    % каждый столбец B — это коэффициенты одной реакции

norm_check = norm(A*B)         % проверка решения: должно получиться примерно 0
                    % если AB ? 0, значит реакция сбалансирована
                    
cond_A = cond(A)
disp('Коэффициенты реакции:')
B

disp('Проверка (A*B):')
A*B